// API utility for fetching stock and correlation data

export async function fetchStockPrices(interval) {
  // TODO: Replace with real API call
  // Example: return fetch(`/api/stocks?interval=${interval}`).then(res => res.json());
  return Promise.resolve([]); // Placeholder
}

export async function fetchCorrelationData(interval) {
  // TODO: Replace with real API call
  // Example: return fetch(`/api/correlation?interval=${interval}`).then(res => res.json());
  return Promise.resolve([]); // Placeholder
} 